# plugin-alexa plugin environment
# Runtime values are loaded from test/config/plugins/plugin-alexa/.env(.local)
# or from process environment (docker-compose environment/env_file).

PLUGIN_ALEXA_ENABLED=true
PLUGIN_ALEXA_LOG_LEVEL=info
